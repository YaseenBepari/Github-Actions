name: Hello world
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: Build step
       run: echo "a demo build"
  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
     - name: test phase
       run: echo "test"
